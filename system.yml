management:
  endpoints:
    web:
      base-path: "{cipher}a41e0aafbfbeea53597ae15cfa2399cdfd366510e9df109d068cb15681fa5116"
      exposure:
        include: "*"
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true
  endpoint:
    shutdown:
      enabled: true
    health:
      probes:
        enabled: true
  info:
    env:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
